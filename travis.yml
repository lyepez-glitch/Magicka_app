language: python

python:
  - "3.11"  # Major Python version only

install:
  - pip install -r requirements.txt  # Install dependencies

before_script:
  - python manage.py migrate  # Run migrations

script:
  - python manage.py test  # Run tests
